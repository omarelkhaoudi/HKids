# Services

Services frontend orientes domaine, au-dessus des appels HTTP de `src/api`.

Garder `src/api` pour les adaptateurs REST simples. Ajouter ici les orchestrations metier: audio, IA, abonnements, controle parental.
