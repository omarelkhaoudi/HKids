# Copy to keystore.properties and fill in before release builds.
# Never commit keystore.properties or the keystore file itself.

storeFile=../release/hkids-release.keystore
storePassword=change-me
keyAlias=hkids
keyPassword=change-me
